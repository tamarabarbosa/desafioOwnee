<div class="navbar navbar-default navbar-static-top active">
      <div class="container">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-responsive-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
       <a><%= link_to "LocOwnee", root_path, "class" => 'navbar-brand' %></a>
        <div class="navbar-collapse collapse navbar-responsive-collapse">
          <ul class="nav navbar-nav">
             <% if user_signed_in? %>
            <li><%= link_to "Novo Filme", new_filme_path  %></li>
            <li><%= link_to "Novo Gênero" , new_genero_path  %></li>

            <% end %>
          </ul>
           <% if user_signed_in? %>
          <ul class="nav navbar-nav navbar-right">
          <div class="btn-group " >
      <button type="button" class=" btn  icon-white icon-user "data-toggle="dropdown">
      Usuario <span class="caret"></span>
      </button>
      <ul class="dropdown-menu" role="menu">
        <li class="divider"></li>
      </ul>
       <a> <%= link_to "Sair", destroy_user_session_path, :method => :delete, :class => 'btn btn-danger ' %></a>
    </div>
            </ul>
            <%end%>
        </div>
      </div>
</div>
