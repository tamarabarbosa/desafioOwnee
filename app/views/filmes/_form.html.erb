<%= form_with(model: filme, local: true) do |form| %>
  <% if filme.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(filme.errors.count, "error") %> prohibited this filme from being saved:</h2>

      <ul>
      <% filme.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="field">
    <%= form.label :genero_id %>
    <%= form.text_field :genero_id %>
  </div>

  <div class="field">
    <%= form.label :preco_de_locacao %>
    <%= form.text_field :preco_de_locacao %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
